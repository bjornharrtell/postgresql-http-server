#!/usr/bin/env ./node_modules/coffeescript/bin/coffee

if not process.env.NODE_ENV then process.env.NODE_ENV = 'production'

path = require 'path'
fs   = require 'fs'
require path.join __dirname, '../lib/cli.coffee'
